package com.atakmap.android.helloworld.features.mapcapture;

import com.atakmap.android.helloworld.abstraction.Handle;

/**
 * Typed handle to a live offscreen render pipeline ({@code OffscreenMapCapture})
 * that {@code MapCaptureCreatorImpl} retains. {@code src/main} holds this, never
 * the GL object itself.
 */
public final class MapCaptureHandle extends Handle {
}
